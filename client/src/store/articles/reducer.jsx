import {
} from '../constants';

const initState = {
  data: [],
};

const articles = (state = initState, {type, payload}) => {

  switch (type) {
    default:
      return state;
  }
};

export default articles;